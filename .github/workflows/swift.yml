name: Swift
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  smoke-test:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v2
    - name: Resolve
      run: swift package resolve -v
    - name: Build
      run: swift build -o MainSwiftUI.app
